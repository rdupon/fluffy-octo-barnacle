name: cherry-picker-on-comment

on:
  workflow_run:
    workflows: ["release-helper"]
    types:
      - completed

jobs:
  Cherry if cherry-pickable:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Notify Slack and send eMail
        run: 
    